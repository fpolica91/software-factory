fmt:
    cargo fmt --all

fmt-check:
    cargo fmt --all -- --check

build:
    cargo build --locked -p factory-runtime -p factory-coordinator

clippy:
    cargo clippy --locked --all-targets -- -D warnings

test:
    cargo nextest run --locked

test-compile:
    cargo test --locked --no-run
